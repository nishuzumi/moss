# Moss的Agent安全规则

这些规则适用于所有使用Moss MCP工具的Agent。

## 必须遵循的流程

1. **记录意图。**在调用工具之前，须完整保留用户的操作意图，包括其使用的原始动词、涉及资产、限制条件、接收方以及协议约束。
2. **先发现，再加载。**选择一个与用户请求的意图及风险相匹配的方法，阅读每个参数的取值约束以及各字段的详细说明。
3. **执行操作。**查询返回数据。写入操作会返回一个由未签名交易组成的Capability树。绝不要自行编造calldata，或修改该树。
4. **模拟执行。**对于所有写入操作生成的Capability树，都必须模拟执行，并且模拟必须在最终执行操作结果返回之后进行。
5. **遇到任何警告，立即停止。**出现以下任意情况时，必须立即终止整个流程：交易回滚、不可用的排序证据、回执失败、结果缺失、变更覆盖率不匹配。
6. **确保有序文本与用户意图一致。**按顺序，将MCP返回的所有回执文本与用户的原始请求进行比对。这些文本仅在底层完整回执通过覆盖率验证后，才能对外暴露。
7. **签名前进行说明。**在签名前，必须向用户解释实际资产、金额、接收方、授权、协议语义、不确定性以及每一条警告。Moss本身不会签名，也不会发送交易。

## 强制规则

- 绝不请求或处理私钥。
- 绝不跳过模拟、重排交易或手动修复Capability树。当输入发生变化时，重新执行操作。
- 绝不忽略警告。也不会为消除警告，而反复执行模拟。
- 绝不将代币符号视为唯一身份标识。必须使用明确的EVM地址或原生资产。如果地址尚不属于受信任的应用上下文，必须让用户给出地址。
- 所有经过MCP的链上数量都必须以十进制字符串表示。单位及换算方式必须遵循已加载参数合约中的单位和转换规则。
- 杜绝仅凭一行摘要就推断成功。必须检查所有按顺序返回的回执文本，并确认每笔交易都属于预期行为。
- 模拟通过，仅证明其对观察变更的详尽解析，并不能证明未来链上执行一定成功，也不能代表用户已经批准交易。用户仍须在钱包中审核交易，同时遵守链上的各项限制。
