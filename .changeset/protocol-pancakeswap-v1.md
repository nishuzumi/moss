---
"@themoss/protocol-pancakeswap": minor
---

Add PancakeSwap V3 single-hop swap adapter for Monad mainnet, with `swap` (Capability) and `quote` (Query), including ERC-20 approval delegation, native MON msg.value forwarding, and typed swap Receipt parser.
