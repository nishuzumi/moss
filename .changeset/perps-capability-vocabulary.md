---
"@themoss/core": minor
---

Extend the closed Capability vocabulary for perpetuals (ADR 0003): category `perps`, position-lifecycle verbs `open` and `close` with direction carried as a Capability parameter, and risk labels `leverage` and `liquidation`. Margin and collateral flows stay on `supply`/`withdraw`.
