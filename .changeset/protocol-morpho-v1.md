---
"@themoss/protocol-morpho": minor
---

Add the Morpho vaults adapter for Monad mainnet: `supply` and `withdraw` Capabilities on MetaMorpho V1.1 vaults, `position` and `vaultInfo` Queries, factory-verified vault identity, and typed Receipt parsers that cover the vault, Morpho Blue, IRM and ERC-20 evidence a vault flow produces.
